## Що змінює

<!-- Одне-два речення по суті. Деталі — у тілі комітів. -->

## Чому

<!-- Issue, симптом, вимірювання. «Що» видно з дифу, «чому» — ні. -->

Closes #

## Як перевірено

<!-- Команда, крок у браузері, скріншот. «Виглядає правильно» — не перевірка. -->

- [ ] `cd backend && DJANGO_SETTINGS_MODULE=naverny_borschu_api.settings_test pytest`
- [ ] `cd frontend && CI=true npx react-scripts test --watchAll=false`
- [ ] Перевірено на мобільній ширині (якщо зачіпає UI)

## Чекліст

- [ ] Гілка від `develop` і PR у `develop` (у `main` — тільки промоушн)
- [ ] Немає секретів, ключів, `.env`, зібраних бандлів чи дампів
- [ ] Нову поведінку прикрито тестом
- [ ] Нові тексти — через `src/i18n/translations.js` (`uk` + `en`)
- [ ] Зміна моделі йде разом з міграцією
